cd /
cd wamp(64)/www/[nom du dossier]
git init
git config --global user.name "***"
git config --global user.email "***"
git add *
git commit -m "1er commit"
git remote add origin ******.git
git push origin master
